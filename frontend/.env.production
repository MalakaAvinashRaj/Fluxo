# Production build — API is served from the same origin via Nginx reverse proxy.
# Empty VITE_API_BASE_URL = relative URLs (browser uses current domain automatically).
VITE_API_BASE_URL=
VITE_API_TIMEOUT=60000
VITE_RETRY_ATTEMPTS=2
VITE_RETRY_DELAY=1000
VITE_LOG_LEVEL=warn
